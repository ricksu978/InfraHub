name: Deploy Dev

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy-dev:
    uses: ./.github/workflows/template-cd.yml
    with:
      environment_name: dev
      project_name: iacdemo
      sql_admin_login: sqladmindev